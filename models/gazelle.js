export default class Gazelle {
    constructor (name, gender, age, color, missingLegs = false){
        this.name=name
        this.gender=gender
        this.age=age
        this.color=color
        this.missingLegs=missingLegs
    }
}